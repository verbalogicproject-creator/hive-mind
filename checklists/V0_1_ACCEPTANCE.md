# v0.1.0 integration acceptance — pending runtime evidence

All checks start pending. Documentation validation cannot mark Android checks complete.

- [ ] Capture returned source archive/hash, dependency versions, schema version and actual test output.
- [ ] Reconcile tools/resources/aliases against V0_1_CONTRACT.md; export Room schemas and test populated v4 upgrade plus fresh creation.
- [ ] Authenticate before dispatch; deny wrong-client/project/lane access across tools, resources, context, cached results, relationships, exports and blobs.
- [ ] Queue metadata omits bodies/excerpts; body reads require exact user-approved version/client/session/project. Editing, revocation or session closure invalidates access.
- [ ] Successful reads create receipts. Agent proposals never become active without user review; stale review inputs fail; original evidence remains.
- [ ] Duplicate request keys return one result; mismatched payload conflicts. Restart preserves unfinished inboxes without automatic promotion/purge.
- [ ] Probe FTS5 on actual driver/device. Run retrieval fixtures and realistic queries; record forbidden leakage, citations, ranks and latency.
- [ ] Context includes framing/citations within budget, reports omissions and never leaks restricted derived content.
- [ ] SAF permission loss, cancellation, caps and partial scans cannot tombstone missing files. Unchanged rescans create no versions; updates activate atomically.
- [ ] Export/restore retains IDs/hashes/visibility/provenance, omits credentials/live grants, and rejects corrupt/traversal archives.
- [ ] Physical-device restart/start/stop works; Stop closes listener.
- [ ] Codex, Claude Code and Antigravity each pass connection, session, scoped search, inbox and handoff flows; record versions, transport and redacted auth setup.
- [ ] Project/inbox/review/evidence screens pass contrast, scalable text, screen-reader, 48dp targets and Hebrew/RTL checks.

For every increment return source/hash, changed interfaces, migration mapping, commands/results, screenshots, known gaps and device/client versions. Never include credentials. Use applicable AppFactory preflight, compile, unit, lint, APK and device checks on the selected returned Android tree. Pipeline repository changes and publication are outside this documentation increment.
