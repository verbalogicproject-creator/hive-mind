# Phases 2 and 3 Brief

Implement in two checkpoints. First add persisted SAF directory grants, resumable scanning, ignores, secret warnings, limits, cancellation, progress, streaming SHA-256 vault, safe writes, versioned Markdown/text/code/config chunkers, FTS5 projection, Hebrew/code tests, and tombstones only after complete scans.

Then add structured exact and BM25 candidate arms, bounded graph expansion, RRF, validity/evidence/status/source filters, bounded adjustments, deduplication/diversity, bounded context with memory URIs, hashes, conflicts, stale warnings, counts, trace, and golden tests. No cloud embeddings; define only the no-embedding baseline interface. No arbitrary paths or unbounded reads. Test each checkpoint and stop.
